# MCP Compact Configuration

# Required: Upstream MCP server endpoint
# MCP_UPSTREAM_URL=http://localhost:8931/mcp

# Optional: LLM server for summarization (if not set, proxy runs in pass-through mode)
# BASE_URL=http://localhost:8000/v1
# MODEL_NAME=openai/gpt-4
# API_KEY=EMPTY

# Optional: Proxy server configuration
# MCP_PROXY_CONFIG_FILE=config.json
# HOST=localhost
# PORT=8009
# LOG_LEVEL=INFO
